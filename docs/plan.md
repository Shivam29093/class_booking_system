## Delivery Plan

1. Keep the backend contract covered by focused lifecycle and authorization tests.
2. Add integration tests against PostgreSQL for booking concurrency, recurrence
	conflicts, dashboard aggregation, CSV output, and membership alert reset.
3. Add production deployment configuration, migrations, secret management, and
	a real frontend build pipeline before release.
